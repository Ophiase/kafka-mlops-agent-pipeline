[project]
name = "dashboard"
version = "0.1.0"
description = "Dashboard application for data visualization"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["django", "kafka-python", "requests", "shared"]

[tool.uv.sources]
shared = { path = "../shared" }