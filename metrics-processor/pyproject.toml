[project]
name = "metrics-processor"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    # "ipykernel",
	"langchain",
    "langchain-ollama",
    "kafka-python"
]
